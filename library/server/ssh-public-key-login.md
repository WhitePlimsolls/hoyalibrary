---
title: SSH 使用公钥连接服务器
---

# SSH 使用公钥连接服务器

### 1. 生成公钥和私钥

命令行输入以下命令，一直按回车

```sh
ssh-keygen
```

生成公钥和私钥会在 `.ssh/` 目录下面

### 2. 进入 `.ssh/` 目录

如果服务器重装系统，需要先删除本目录下 `know_hosts`

```sh
cd ~/.ssh
```

### 3. 将公钥传送到远程主机 host 上面

按照提示输入密码

```sh
ssh-copy-id [user]@[host]
```
